import type { PlayerStats } from '../../types'

// PLACEHOLDER - implement when ready
export const processPlayerData = async (): Promise<PlayerStats[]> => {
  return [];
}

export const getPlayerValue = (player: PlayerStats, statKey: string): number => {
  return 0;
}

export const getPlayerPercentileRank = (player: PlayerStats, statKey: string): number => {
  return 0;
}