import type { MatchStats } from '../../types'

// PLACEHOLDER - implement when ready
export const processMatchData = async (): Promise<MatchStats[]> => {
  return [];
}

export const getMatchValue = (match: MatchStats, statKey: string): number => {
  return 0;
}