import type { MatchStats } from '../../types'

// PLACEHOLDER - implement when ready
export function useMatchData() {
  return {
    data: [] as MatchStats[],
    isLoading: false,
    error: null
  }
}