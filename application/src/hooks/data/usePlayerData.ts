import type { PlayerStats } from '../../types'

// PLACEHOLDER - implement when ready
export function usePlayerData() {
  return {
    data: [] as PlayerStats[],
    isLoading: false,
    error: null
  }
}